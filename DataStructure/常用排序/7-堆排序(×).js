void function() {
  const arr = [3, 2, 1, 5, 6, 4];

  // 主要运用队列
  function heapSort(arr) {
    if(arr.length <= 1) {
      return arr;
    }

    return arr;
  }

  const res = heapSort(arr);
  return res;
}()